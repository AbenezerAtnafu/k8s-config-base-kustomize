apiVersion: v1
kind: ResourceQuota
metadata:
  name: app-stage-quota
  labels:
    app.kubernetes.io/part-of: bsis
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/environment: stage
spec:
  hard:
    requests.cpu: "3"
    requests.memory: 4Gi
    limits.cpu: "12"
    limits.memory: 8Gi
    pods: "40"
    services: "15"
    configmaps: "40"
    secrets: "60"
