apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bsis-app-staging
  namespace: argocd
spec:
  project: bsis
  source:
    repoURL: https://github.com/HABTec/bsis-k8s.git
    targetRevision: main
    path: apps/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: app-staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
